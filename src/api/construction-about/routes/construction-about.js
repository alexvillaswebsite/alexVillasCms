'use strict';

/**
 * construction-about router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::construction-about.construction-about');
