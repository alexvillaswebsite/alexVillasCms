'use strict';

/**
 * construction-about controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::construction-about.construction-about');
